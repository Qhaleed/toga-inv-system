To start frontend:
    cd frontend:
        npm start
To start backend:
    cd backend:
        npm install
        npm install dotenv
        npm run startDev // This will enable nodemon(hot reload package)
        or use this
        node server.js

To Start temporary database of json for tables:

cd toga-inv-system/frontend/src/components/admin-dashboard
then 
npx json-server --watch sample.json --port 8000
or
Direct using these: npx json-server --watch frontend/src/components/admin-dashboard/sample.json --port 8000

Login Guide:
    Hardcoded pa yung mga accounts natin atm:
    
        const adminAccounts = [
            { email: "admin123@gmail.com", password: "password123" },
            { email: "admin@toga.edu", password: "admin2024" }
        ];

        const studentAccounts = [
            { email: "student1@toga.edu", password: "student123" },
            { email: "student2@toga.edu", password: "student456" }
        ];

dang tailwind setup is shite